import { QRCode as QR_QRCode } from "jsqr/dist";
import { Point as QR_Point } from "jsqr/dist/locator";
import { Chunks as QR_Chunks } from "jsqr/dist/decoder/decodeData";
export interface QRCode extends QR_QRCode {
}
export interface Point extends QR_Point {
}
export interface Chunks extends QR_Chunks {
}
export interface BaseConfig {
    src?: string;
    isAuto?: boolean;
    isDraw?: boolean;
    isBeep?: boolean;
    isAlwaysEmit?: boolean;
    medias?: MediaStreamConstraints;
}
export interface ScannerQRCodeResult {
    canvas?: HTMLCanvasElement;
    file?: File;
    name?: string;
    url?: string;
    data?: string;
    binaryData?: number[];
    chunks?: Chunks;
    version?: number;
    location?: {
        topRightCorner?: Point;
        topLeftCorner?: Point;
        bottomRightCorner?: Point;
        bottomLeftCorner?: Point;
        topRightFinderPattern?: Point;
        topLeftFinderPattern?: Point;
        bottomLeftFinderPattern?: Point;
        bottomRightAlignmentPattern?: Point;
    };
}
